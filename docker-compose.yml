services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bptt
      POSTGRES_USER: bptt
      POSTGRES_PASSWORD: bptt
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
